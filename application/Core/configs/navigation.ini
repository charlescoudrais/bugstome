[production]

index.label = 'NAV_HOME'
index.route = index
index.visible = true
index.order = 1

task.label = NAV_TASKS
task.route = task
task.visible = true
task.order = 2

project.label = NAV_PROJECT
project.route = project
project.visible = true
project.order = 3

stat.label = NAV_STAT
stat.route = stat
stat.visible = true
stat.order = 4

[development : production]
[testing : production]
[staging : production]